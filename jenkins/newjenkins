pipeline{
     agent {
     label 'node1'
     }stages{
           stage('stage1'){
                 steps{
                      sh '''
                              echo "stage1 is completed `hostname`"
                          '''
                 }
           }
        }
}

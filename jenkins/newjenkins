pipeline{
     agent {
        label 'node2'
     }
      stages{
           stage('stage1'){
                 steps{
                      sh '''
                              echo "stage1 is completed `hostname`"
                          '''
                 }
           }
        }
}
